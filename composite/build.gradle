wrapper {
    gradleVersion = "4.2.1"
    distributionType = Wrapper.DistributionType.ALL
}

subprojects {
    group 'com.example'
    version '1.0-SNAPSHOT'

    apply plugin: 'maven'
    apply plugin: 'java'

    sourceCompatibility = 1.8

    repositories {
        mavenLocal()
    }
}